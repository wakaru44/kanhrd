# add-pane-tab-hierarchy

Give the pane view herdr's two levels: a tab strip over the workspace's tabs, with the existing card switcher as the level beneath it, and make prefix+n / prefix+p move between tabs there instead of silently moving the board's scope.
