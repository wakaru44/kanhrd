# add-tier-3-lifecycle

Freeze tier-3 pane/tab/workspace lifecycle wire contract: browser-driven pane split/close/move and tab/workspace create/rename/close, reusing herdr's own native lifecycle events (no bridge-synthesized polling needed, unlike tier-2's pane.output).
