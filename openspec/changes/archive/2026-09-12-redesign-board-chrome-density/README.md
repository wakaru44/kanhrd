# redesign-board-chrome-density

Lay the filter bar out as one wrapping row of labelled groups instead of three stacked rows, so the board's chrome stops taking 43% of a laptop viewport and a host chip is still tellable from a status chip.
