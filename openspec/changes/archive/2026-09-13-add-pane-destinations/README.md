# add-pane-destinations

Give every create and move a chosen destination, and rebuild the card's action row as move / split / rest / dots, so a new card lands where the operator is looking and a pane can be relocated between tabs and workspaces.
