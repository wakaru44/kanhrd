# add-parked-column-reorder

Let the operator reorder parked columns: a keyboard path in the column header menu, and a horizontal column drag with the header as the handle. Status columns stay fixed.
