# add-markdown-remark-rendering

Replace the hand-rolled markdown block parser with a unified/remark pipeline rendered through Angular templates, with a registration seam for future fence and node renderers
