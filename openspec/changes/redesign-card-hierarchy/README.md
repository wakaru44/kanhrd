# redesign-card-hierarchy

Rebuild the board card as nested row and column boxes rather than one shared grid, so the title owns its line, and stack the locators at most two per line instead of truncating three.
