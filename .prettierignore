# `openspec validate --strict` is a stronger gate than prettier; ADRs and
# history are frozen once written. See .pre-commit-config.yaml for the same
# scope note. `apps/` and `packages/` used to sit here as a fence around
# concurrent lanes — they are formatted like everything else now.
openspec/
docs/adr/
README.md

# Claude Code skill/command definitions: prompt structure, not prose docs.
.claude/

# Frozen historical artifacts — captured verbatim, do not reformat.
docs/history/

# Generated / vendored.
node_modules/
pnpm-lock.yaml
dist/
build/
coverage/
